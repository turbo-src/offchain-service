import { Repo, Vote } from "../../server/db/index.js";
export default async function getVoteYesTotals(
  /*owner:*/ owner,
  /*repo:*/ repo_id,
  /*pr_id:*/ pr_id,
  /*contributor:*/ contributor_id,
  /*side:*/ side
) {
  //Get all votes for pr reduce method to return total
}
