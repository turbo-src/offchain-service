const { Repo, Vote } = require("../../server/db");
async function getVoteYesTotals(
  /*owner:*/ owner,
  /*repo:*/ repo_id,
  /*pr_id:*/ pr_id,
  /*contributor:*/ contributor_id,
  /*side:*/ side
) {
  //Get all votes for pr reduce method to return total
}
module.exports = getVoteTotals;
